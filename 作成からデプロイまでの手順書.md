# 耳読ライフ 実環境シート・構築記録

**手順の本体は `D:\site\手順書-CloudflareKV構成.md`（汎用版）にある。このファイルは値とこのサイト固有の事情だけを持つ。同じ手順を写さない。**

構成を選んだ理由・判断の背景は `C:\Users\elsee\repos\second-brain\notes\Cloudflare-Worker+KV構成メモ.md`。

> **コマンドはすべて `D:\site\mimidoku-life-kv-site` を開いた状態で実行する。**このファイル内のコマンドは PowerShell / Bash どちらでもそのまま通る形にしてある（`&&` で繋がない・`cd` を混ぜない）。PowerShell 5.1 は `&&` を解釈できない。

---

## 実環境シート

| 項目 | 値 |
|---|---|
| 公開URL | https://mimidoku-life.com/ |
| Pagesプロジェクト | `mimidoku-life-kv-site`（GitHub連携。旧 `mimidoku-life` は2026-09-08に削除） |
| リポジトリ | `mtyk-so/mimidoku-life-kv-site`（プロダクションブランチ `main`） |
| Worker | https://mimidoku-life-kv-cms.mtyk-so.workers.dev/ |
| KV namespace | `mimidoku-life-cms-data` |
| KV namespace ID | `worker/wrangler.jsonc` を参照（ここには書かない） |
| KV binding | `CMS_KV` |
| データファイル | `items.json` `tags.json` |
| 供給側 | `supply/gas/Code.gs`（**未実装**。`getItemsFromSheet()` が空配列を返す） |
| Access Application | `mimidoku-life-kv-site`（宛先：`mimidoku-life.com` と `mimidoku-life-kv-site.pages.dev`、Bypass/Everyone） / `mimidoku-life-kv-cms.mtyk-so.workers.dev`（Bypass/Everyone）。**2026-09-08にアカウントの既定が公開へ反転したため、これらが無くても公開される**（下の2026-09-08節） |
| アフィリエイト | A8.net「AmazonのオーディオブックAudible」 |
| 旧サイト名 / 旧スラッグ | 耳読ナビ / `mimidoku-navi`（2026-09-04に改名。理由は下の「サイト名の変更」節） |
| 構築日 | 2026-09-03 |
| 最終更新 | 2026-09-08 |
| 現況 | **2026-09-08に独自ドメインで公開。**リニューアル版（記事2本・新デザイン）が本番。`/works`（旧作品一覧）は削除済みで、**WorkerとKVは現在どこからも使われていない**。KVのデータは残っている（全5件に `impression` フィールドあり、中身は未記入） |

---

## ページ構成

| ファイル | 役割 |
|---|---|
| `public/index.html` | **トップ**。登録・使い方・解約ガイド（2026-09-04〜。旧guide.htmlの内容） |
| `public/works.html` | 気になる作品ピックアップ（検索・タグ絞り込み。旧トップの作品一覧、2026-09-04に分離） |
| `public/404.html` | 404 |
| `public/js/config.js` | サイト共通設定。**Worker URL・A8素材はここ1箇所** |
| `public/js/api.js` | KV/Workerからのデータ取得層 |
| `public/js/site.js` | 全ページ共通のユーティリティ（エスケープ・アフィリリンク適用・バナー・計測タグ） |
| `public/data/` | ローカル確認用のサンプルJSON |
| `worker/src/index.js` | Worker本体（`/health` `/data/*` `/import`） |
| `supply/gas/Code.gs` | GASからKVへ投入する供給側 |
| `server.mjs` | ローカル確認用サーバー（`http://127.0.0.1:4173/`。`npm start`） |
| `tools/import-to-kv.mjs` | `public/data/*.json` をKVへ投入する（`npm run import`） |

---

## このサイト固有の事情

### A8.netの素材は「リンク」と「計測ピクセル」を対で持つ

A8の広告素材はIDごとに `a8mat` が違い、**表示回数計測タグ（1x1画像）のホスト名も素材ごとに違う**。テキストとバナーで同じ `a8mat` を使い回すことはできない。

| 素材 | 用途 | a8mat | 計測ピクセル |
|---|---|---|---|
| ID:001 テキスト | ヒーローCTA・ガイドのCTA・作品カードの「Audibleで聴く」 | `...+5YJRM` | `www13.a8.net/0.gif` |
| ID:006 バナー300×250 | トップの作品一覧下・ガイド末尾 | `...+5ZMCH` | `www16.a8.net/0.gif` |

`config.js` の `A8.text` / `A8.banner` に `link` と `impression` をセットで置いてある。**素材を差し替えるときは必ず両方を入れ替える。**

計測ピクセルは `site.js` が「そのページに実際に出した素材のぶんだけ」出力する。バナーを置いていないページでバナーの計測タグだけ焚く、という水増しを避けるため。

**バナーに `loading="lazy"` を付けない。**計測ピクセルだけ先に発火してバナー本体が読み込まれず、表示回数が実態からずれる。

### KVのデータはPagesの再デプロイ不要で即時反映される

作品の追加・リンク差し替えは `/import` へPOSTするだけ。Pagesのデプロイは走らない。逆に、HTML/CSS/JSを直したときはデプロイが必要。

### ガイド本文（index.html）に料金を書いていない

Audibleの料金・無料体験の期間・聴き放題の対象作品数は変わる。数字を書くと古くなった瞬間に嘘になるため、手順とFAQだけを書き、条件は「公式サイトで最新を確認」に寄せてある。

---

## 2026-09-04: サイト構成の見直し（トップ=ガイド、作品一覧は分離）

### 何を変えたか

- `public/index.html`（トップ）の中身を、旧`guide.html`（登録・使い方・解約ガイド）に入れ替えた
- 旧トップの作品一覧は `public/works.html` に分離。見出しは「管理人のおすすめ厳選作品」→**「気になる作品ピックアップ」**に変更
- 旧`guide.html`は削除。ナビ・ヒーローCTAのリンク先はすべて新構成に合わせて更新済み
- `items.json` の各作品に **`impression` フィールド**（今は全件 `""`）を追加。`works.html` のカードは、空なら「感想：これから聴いて追記していきます」を薄い色で表示し、値が入れば本文をそのまま表示する
- 5作品の `description` から、「聴くだけで〜になる」「必聴」「必読書」といった**聴いた前提の断定表現**を外し、内容の客観的な紹介に書き換えた。バッジ（本屋大賞受賞作等）は客観的事実なのでそのまま残した

### なぜ変えたか

サイト運営者（自分）がAudibleをまだ聴いていない段階で、「管理人のおすすめ」という個人の体験に基づく推薦を装ったトップページを出すのは実態と合わない、という指摘から。ベースにした構築手順書・Ingest済みレポートがこの構成（トップ＝作品一覧）を採っていたのは、分析対象のサイトが実際に経験を積んだ運営者だったから成立していただけで、自分の状況には当てはまらないと判断した。

### 今後の方針（B寄り）

経験の無さを隠すのではなく、**「これから聴き始める」自体をコンテンツにする**方向で進める。具体的には、既に確立している音声→ブログ記事ワークフロー（テーマ一言→話す見出し提案→録音→文字起こし整形→即保存）を、Audibleで聴いた分のメモに転用する。聴くたびに `impression` フィールドが埋まり、サイトのコンテンツが本物の一次情報として増えていく想定。

**次にやる実質的な一手**：作品を1冊選んで実際に聴く→音声ワークフローでメモを作る→その作品の `impression` を埋めて下の「データを更新する」コマンドで`/import`する。

---

## 2026-09-04: サイト名の変更（耳読ナビ → 耳読ライフ）

### なぜ変えたか

「耳読ナビ」は既に同名のサイトが存在することが判明したため（ユーザーによるリサーチ済み）。名前とスラッグを以下へ変更する。

| | 旧 | 新 |
|---|---|---|
| サイト名 | 耳読ナビ | 耳読ライフ |
| スラッグ | `mimidoku-navi` | `mimidoku-life` |
| Pages | `mimidoku-navi.pages.dev` | `mimidoku-life.pages.dev` |
| Worker | `mimidoku-navi-kv-cms` | `mimidoku-life-kv-cms` |
| KV namespace | `mimidoku-navi-cms-data` | `mimidoku-life-cms-data` |
| ローカルのディレクトリ | `D:\site\mimidoku-navi-kv-site` | `D:\site\mimidoku-life-kv-site` |
| Access Application名 | `Public - mimidoku-navi` / `... API` | `Public - mimidoku-life` / `... API` |

### ローカル側（2026-09-04 完了済み）

- ディレクトリを `mimidoku-life-kv-site` にリネーム
- `public/js/config.js` の `SITE_NAME` と `WORKER_BASE_URL`
- `public/index.html` `works.html` `404.html` の title・ヘッダー・フッター
- `worker/wrangler.jsonc` `worker/wrangler.example.jsonc` の `name`
- `package.json` の `name`
- この手順書と、Vault側の `.claude/launch.json`・`notes/Cloudflare-Worker+KV構成メモ.md`

**KV namespace ID は変えていない。**バインディングはIDで解決されるため、namespaceの表示名を変えても中のデータはそのまま引き継がれる。

### Cloudflare側の移行手順

**PagesプロジェクトとWorkerは名前を変更できない。**新しい名前でデプロイすると別リソースとして新規作成される。旧リソースは残るので、確認後に自分で消す。順番を守らないと、新しいホストがAccessに塞がれたまま公開されない状態になる。

移行中は**旧サイトが200のまま生きている**ので、公開が途切れることはない。

1. ~~**KV namespaceの表示名を変える**~~ → **2026-09-04 実施済み。**IDは `ceab2cd...` のまま変わらず、データもそのまま。`--namespace-id` を使うのが確実（`[old-name]` 位置引数もあるがIDのほうが取り違えない）。
   ```bash
   npx wrangler kv namespace rename --namespace-id <KV namespace ID> --new-name mimidoku-life-cms-data
   ```
2. ~~**新Workerをデプロイする**~~ → **2026-09-04 実施済み。**`wrangler.jsonc` の `name` が新しいので、旧Workerとは別物として新規作成される。バインディングが同じKV IDを指していることがデプロイ出力に出る。
   ```bash
   npx wrangler deploy --config worker/wrangler.jsonc
   ```
3. ~~**新Workerに `CMS_IMPORT_SECRET` を登録する**~~ → **2026-09-04 完了。**手順9のPOSTが200を返したことで確認できた。シークレットはWorkerごとに持つため、旧Workerの値は引き継がれない。シークレットはWorkerごとに持つため、旧Workerの値は引き継がれない。旧Workerと同じ値でよい。
   ```bash
   npx wrangler secret put CMS_IMPORT_SECRET --config worker/wrangler.jsonc
   ```
4. ~~**Zero Trust に Worker用のBypassアプリを作る**~~ → **2026-09-04 実施済み。**アプリ名は `mimidoku-life-kv-cms.mtyk-so.workers.dev`、Destination同名、Policy は Bypass / Everyone。
5. ~~`/health` が200を返すことを確認する~~ → **2026-09-04 200を確認。**
   ```bash
   curl.exe -s -o NUL -w "%{http_code}\n" https://mimidoku-life-kv-cms.mtyk-so.workers.dev/health
   ```
6. ~~**新Pagesプロジェクトを作ってデプロイする**~~ → **2026-09-04 実施済み。****Pagesは`deploy`では自動作成されない**（Workerと違う点。`does not exist` エラーになる）。`project create` を先に打つ。
   ```bash
   npx wrangler pages project create mimidoku-life --production-branch=main
   npm run deploy
   ```
7. ~~**Zero Trust に Pages用のBypassアプリを作る**~~ → **2026-09-04 実施済み。**アプリ名 `mimidoku-life.pages.dev`。**Worker側の1つだけでは足りない**という詰まった点1の罠は、今回は最初から2つ作って回避した。
8. ~~トップが200を返すことを確認する~~ → **2026-09-04 確認済み。**トップ・`/works` とも200、作品カード5件がKV経由で描画されるところまで見た。なお `/works.html` は308で `/works` へ寄せられる（Pagesの既定動作。旧サイトでも同じ）。
9. ~~**データを投入する**~~ → **2026-09-04 完了。**`npm run import` で `200 {"success":true}`。5件すべてに `impression` キーが入り、説明文も新しいものへ入れ替わったことを本番の `/data/items.json` と `works` の実表示で確認した。
10. ~~**旧リソースを削除する**~~ → **2026-09-04 完了**（Accessアプリ2つを除く）。新URLの表示を確認したうえで実行した。
    ```bash
    npx wrangler pages project delete mimidoku-navi --yes
    npx wrangler delete --name mimidoku-navi-kv-cms --force
    ```
    **KV namespaceはWorkerを消しても残る。**アカウント単位の別リソースのため。削除後に `kv namespace list` で `mimidoku-life-cms-data`（ID `ceab2cd...`）が健在であること、新サイトの4エンドポイントが200のままであることを確認済み。
    削除後の旧URLは **旧Pages＝530 / 旧Worker＝404**。
    旧Accessアプリ2つ（`mimidoku-navi.pages.dev` / `mimidoku-navi-kv-cms...`）はCLIから消せないためDashboardで削除。**2026-09-04に実施済み。**

**現在地：移行完了（2026-09-04）。**1〜10すべて完了し、A8の登録サイト情報も更新済み。旧名のリソースは残っていない。

### A8.netの登録サイト情報

**A8には提携時に申請したサイトのURLが登録されている。**ドメインが `mimidoku-navi.pages.dev` から `mimidoku-life.pages.dev` へ変わるため、A8の管理画面で登録サイトのURL（とサイト名）を更新する必要がある。**2026-09-04に更新済み。**広告素材の `a8mat` 自体は変わらないため、`config.js` のリンク・計測ピクセルは差し替え不要だった。

---

## 2026-09-08: 独自ドメイン公開とGitHub連携への移行

### 何を変えたか

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 公開URL | `mimidoku-life.pages.dev` | `mimidoku-life.com` |
| Pagesプロジェクト | `mimidoku-life`（直接アップロード） | `mimidoku-life-kv-site`（GitHub連携） |
| デプロイ | 手元から `npm run deploy` | pushでCloudflareがビルド |
| 公開スイッチ | 手元のビルドコマンドを使い分け | Pagesのビルド設定を切り替え |
| Accessの既定 | 保護（作ったものはログイン画面） | **公開（作ったものは素通し）** |
| 旧作品一覧 `/works` | 稼働 | 削除 |

### なぜ変えたか

独自ドメインを付けたところ、Accessのログイン画面から抜けられなくなった。原因は `All Workers` というアプリ（宛先「すべてのWorkers」、ポリシー `Cloudflare account members`）がアカウント内のPagesとWorkersを全部覆っていたこと。**個別のBypassアプリは `*.pages.dev` には効くが、Pagesのカスタムドメインには効かない**ため、`All Workers` 本体のポリシーを `Bypass` / `Everyone` に差し替えて解決した。

その過程でPagesプロジェクトを作り直す必要が生じたため、直接アップロードからGitHub連携へ同時に移行した。**直接アップロードで作ったプロジェクトは、後からGit連携に切り替えられない。**

### Accessで踏んだ落とし穴

- **アプリの削除は、開ける操作ではなく閉じる操作。**削除するとポリシーだけが消えてホスト名の紐づけは残り、対象は「管轄内だが許可なし」になって全員が締め出される。開けたいときは削除ではなく編集する
- `All Workers` は宛先が「すべてのWorkers」としか表示されず、ドメイン名が出ない。**アプリ一覧をホスト名で検索しても見つからない**
- 犯人のアプリは、302のLocationに埋まっているAUDを復号し、各アプリの「追加設定 > AUDタグ」と突き合わせて特定する

```bash
curl -sI https://<host>/ | grep -i '^location:' | grep -o 'meta=[^&]*' | sed 's/meta=//' | cut -d. -f2 | tr '_-' '/+' | base64 -d
```

- 緩める順序を間違えない。**先に守りたいホストを個別アプリで囲い、そのあとで `All Workers` を緩める。**逆にすると一瞬でも公開状態になる。今回は `vault-dashboard-d7x.pages.dev` を先に囲った

### 公開スイッチ

Pagesのビルドコマンドで切り替える。

| ビルドコマンド | 生成物 |
|---|---|
| `npm run build` | 確認版。noindex、確認用の帯、A8計測タグなし、`robots.txt` は全拒否 |
| `npm run build:release` | 公開版。上記が外れ、`robots.txt` は許可＋sitemapの記載、A8計測タグを1ページにつき1点出力 |

**現在は `build:release`。**非公開に戻すときはこの1行を変えて再デプロイする。

### あわせて実施

- canonical・OGP・`robots.txt`・`sitemap.xml` を `tools/build-site.mjs` から出力するようにした。サイトの起点URLは同ファイルの `siteUrl`
- 内部リンクを拡張子なし（`/articles/<slug>`）に変更。308転送が1回減った
- `photo/`（写真の元データ）を `.gitignore` に追加
- A8の登録サイトURLを `https://mimidoku-life.com/` に修正（サイトID `002` のまま。素材の `a8mat` は変更不要、提携も継続）
- Search Consoleにドメインプロパティを登録し、`https://mimidoku-life.com/sitemap.xml` を送信（3ページ検出）

詳細は `docs/2026-09-08-公開までの変更点.md`。判断の背景はVault側の `notes/Cloudflare-Worker+KV構成メモ.md`。

---

## 構築の流れ（2026-09-03）

汎用手順書の §5〜§10 に対応する。ここには**実際にやった順と、詰まった点だけ**を書く。

※ 名前は2026-09-04の改名後の値（`mimidoku-life`）に置き換えてある。当時は `mimidoku-navi` で作成した。手順そのものは変わらないため、そのまま再実行できる形にしてある。

1. ローカルで画面確認（`node server.mjs` → `http://127.0.0.1:4173/`）
2. KV namespace 作成（`npx wrangler kv namespace create mimidoku-life-cms-data`）
3. `CMS_IMPORT_SECRET` を登録し、Workerをデプロイ
4. Zero Trust に `Public - mimidoku-life API`（Bypass / Everyone）を作成 → `/health` が200
5. Node経由でKVへ初期JSONを投入
6. Pagesへ公開
7. guide.html追加・index.html改修
8. A8提携申請 → 本番リンクと計測タグを反映
9. Zero Trust に `Public - mimidoku-life`（Bypass / Everyone）を作成 → **Pagesが200になった**
10. `npx wrangler pages deploy public --project-name=mimidoku-life --branch=main` で本番反映（**この経路は2026-09-08に廃止。**現在はpushでCloudflareがビルドする）
11. `CMS_IMPORT_SECRET` をローテーション

### 詰まった点1: Pagesだけが302になる（手順9）

**Workerに1つBypassアプリを作っただけでは、Pagesは公開されない。**`/data/items.json` は200で返るのにトップページはログイン画面へ転送される、という紛らわしい状態になる。アカウント全体のAccessが両方を塞いでいるため、**公開するホストごとに1つずつ**Bypassアプリが要る。

汎用手順書の付録Bには最初から `Public - <site> / Public - <site> API` と2つ書いてあった。このサイトの初期手順書がWorker側だけを記録していたため、Pages側が抜けた。

なお、Pagesプロジェクトの `Settings > General > Preview access` は**プレビューURL専用**で、本番の `.pages.dev` には効かない。画面にもそう書いてある。ここを触っても解決しない。

### 詰まった点2: 現行UIのメニュー位置

Zero Trustは「Cloudflare One」へ改称され、`Access > Applications` は**左サイドバーの `Access controls > Applications`** に移った。見つからないときは Quick search（ctrl+K）で `Applications`。

Destinationsの Domain プルダウンには `pages.dev` が出てこない（自分が登録したゾーンの一覧のため）。**「Switch to custom input」に切り替えて `mimidoku-life.pages.dev` を直接入力する。**

ポリシーは Action を **Bypass**（Allowではない）、Include のセレクタを **Everyone**。

### 詰まった点3: ディレクトリが二重になっていた

`mimidoku-navi-kv-site/mimidoku-navi-kv-site/`（当時のディレクトリ名）に古いコピーがあり、**`wrangler.jsonc` と `.wrangler/` は入れ子側にしか無い**状態だった。編集はルート側、Workerのデプロイは入れ子側、という食い違いが起きる。2026-09-03に統合し、旧コピーは `D:\site\_old-copy-mimidoku-20260903\` へ退避した。

---

## 運用

### データを更新する

`public/data/*.json` を直してから、投入スクリプトを走らせる。

```bash
npm run import
```

中身の要約（件数・impression記入済み件数・先頭作品の説明文）を出して y/N を聞き、そのあとシークレットを**画面に出さない形で**尋ねる。`200 {"success":true}` なら成功。

**シェルの1行コマンドで済ませようとしない。**以前ここにBashのワンライナー（`read -s -p ... && export ... && node -e "..."`）を書いていたが、**PowerShellでは `&&` も `read -s` も使えず**そのまま貼ると `トークン '&&' は、このバージョンでは有効なステートメント区切りではありません` で落ちる。JSを `-e` に埋める形は引用符の相性も悪い。スクリプト（`tools/import-to-kv.mjs`）に寄せてあるのはこのため。

スクリプト側に置いてあるガード:

- `items.json` か `tags.json` が空なら中止する（空配列で上書きすると**公開中の作品が全部消える**）
- 投入前に要約を出して y/N を聞く
- シークレットは対話入力。コマンド文字列にも履歴にも残らない
- 401 のときは「改名時に新Workerへ登録した値を使う」と出す

### 記事・見た目・文言を更新する

**2026-09-08にpush運用へ変わった。**手元からのデプロイは行わない。

1. `content/articles/<slug>.md` を編集する（記事を追加するときは `content/site.json` にも1件足す）
2. 画像は `public/images/` に置く（`photo/` はgitignore対象でデプロイされない）
3. `npm run build` してから `npm start` で `http://127.0.0.1:4173/` を確認
4. コミットしてpush。Cloudflareがビルドして本番へ反映する

**`public/` の生成物はCloudflare側のビルドで上書きされる。**手元でコミットした生成物は本番に反映されないため、本文の修正は必ず `content/` 側で行う。

デザインの共通部分（head、カード、記事テンプレート、canonical、OGP、sitemap）は `tools/build-site.mjs` にある。

### 公開状態を確認する

```bash
curl.exe -s -o NUL -w "%{http_code} %{redirect_url}\n" -I https://mimidoku-life.com/
```

`200` が正しい。`302` かつ `cloudflareaccess.com` へ転送されるならAccessが掛かっている。

**2026-09-08以降、アカウントの既定は公開。**そのため「公開したいのに302」より「非公開にしたいのに200」のほうが起きやすい。非公開のものを作ったときは、そちらの302を必ず確認する。

公開版ビルドかどうかは `robots.txt` で見分ける。`Allow: /` と `Sitemap:` が出れば公開版、`Disallow: /` なら確認版のまま。

---

## 残っている作業

**2026-09-08追記：`/works` を削除したため、以下のKV・GAS関連の項目は「作品一覧を作り直すなら」という前提になった。**現在サイトはWorkerを参照していないので、着手の緊急性はない。作り直さないと決めるなら、Worker・KV名前空間・GAS連携・Accessアプリをまとめて畳む判断になる。

- **作品を実際に1冊聴いて、`impression`を埋める。**「2026-09-04: サイト構成の見直し」節の「今後の方針（B寄り）」参照。音声→ブログ記事ワークフローをそのまま転用できる。
- **GASの `getItemsFromSheet()` が未実装。**スプレッドシート連携はまだ動かない。空配列でKVを上書きすると公開中の作品が全部消えるため、`syncDataToKV()` の先頭にガード（空なら中止）を入れてある。実装したらガードは自然に通過する。
- **タグ「ビジネス・仕事術」に該当作品が0件。**押すと空表示になる。
- **GASのスクリプトプロパティ `CMS_IMPORT_SECRET`** を現行の値へ更新し、投入先URLも新Worker（`mimidoku-life-kv-cms`）へ向け直す。2026-09-03のローテーションと2026-09-04の改名の両方が未反映（GAS側の供給スクリプトは未実装なので緊急性は低い）。

---

## セキュリティ上のメモ

- `CMS_IMPORT_SECRET` は `/import` を守る唯一の鍵。この1本だけで、KVのデータ（＝全作品のアフィリエイトリンク）を書き換える権限が守られている。リンクを差し替えられても見た目は変わらず、デプロイもgit履歴も残らないため気づきにくい。
- 2026-09-03に旧値からローテーション済み。旧値でのPOSTは `401` を返すことを確認した。
- `wrangler.jsonc` と `.wrangler/` は `.gitignore` 済み。KV namespace IDはこのファイルには書かない。
- KV由来のテキストは `site.js` の `escapeHtml()` を通してから描画する。`link` は `safeLink()` が `http/https` 以外を弾く。
